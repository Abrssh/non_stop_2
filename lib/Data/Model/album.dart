class Album {
  final String name, artist, smallImageUrl, largeImageUrl, spotifyUrl, date;

  Album({
    required this.name,
    required this.artist,
    required this.smallImageUrl,
    required this.largeImageUrl,
    required this.spotifyUrl,
    required this.date,
  });
}
